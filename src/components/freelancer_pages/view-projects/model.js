import { Record } from 'immutable';

export const Projects = Record({
   activeRequests:[],
   recruitingRequests: [],
   locationFilterData: [],
   hourlyRateFilterData: [],
   isLoading: true,
  });